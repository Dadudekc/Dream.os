## 🔁 REFACTOR FLAGS (TDD)
- Enforce DI: {{ enforce_dependency_injection | default(true) }}
- Use Async: {{ require_async | default(false) }}
{# Note: Modularization and Test generation are inherent to TDD workflow #} 